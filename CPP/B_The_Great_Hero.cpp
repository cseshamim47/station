#include <bits/stdc++.h>
using namespace std;

#define BOOST ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);
#define endl "\n"
#define w(t) int t; cin >> t; while(t--){ solve(); }
#define int long long
#define MAX 1000006

// bool cmp(pair<int,int> a,pair<int,int> b)
// {
//     if(a.first < b.first) return true;
//     else return false;
// }

void solve()
{
    // int hp,hh,n;
    // cin >> hp >> hh >> n;
    // int mp[n]; 
    // int mh[n]; 
    // for(int i = 0; i < n; i++) cin >> mp[i];
    // for(int i = 0; i < n; i++) cin >> mh[i];
    // vector<pair<int,int>> v;
    // for(int i = 0; i < n; i++)
    // {
    //     v.push_back({mp[i],mh[i]});
    // }
    // sort(v.begin(),v.end(),cmp);

    // for(int i = 0; i < n; i++)
    // {
    //     // cout << ceil((double)v[i].second/hp) << " " << v[i].first << endl;
    //     if(hh <= 0 && n != 1)
    //     {
    //         cout << "NO" << endl;
    //         return;
    //     }
    //     int curHealthOfHero = hh - ceil((double)v[i].second/hp)*v[i].first;
    //     int curHealthOfEnemy = v[i].second - ceil((double)hh/v[i].first)*hp;
    //     hh = curHealthOfHero;
    //     v[i].second = curHealthOfEnemy;

    //     if(curHealthOfHero < 1 && curHealthOfEnemy > 0)
    //     {
    //         cout << "NO" << endl;
    //         return;
    //     }
    //     // cout << hh << endl;
    //     // getchar();
    // }
    // cout << "YES" << endl;
    // // for(auto p : v) cout << p.first << " " << p.second << endl;

    
    // damage += monHealth[i]
}

int32_t main()
{
      //        Bismillah
    // BOOST
    w(t)
    //solve();
}