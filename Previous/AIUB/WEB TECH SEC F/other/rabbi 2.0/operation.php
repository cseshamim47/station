<?php

require_once('db.php');

/* function adduser($insert) {
    $con = getConnection();
    $sql = "insert into who values('{$name['u_name']}','{$email['u_email']}','{$pass['password']}')";
    $status = mysqli_query($con, $sql);
    return $status;
} */

?>